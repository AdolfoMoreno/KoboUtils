const { expect } = require('chai');
const path = require('path');

describe('Annotation Reader', () => {
    it('should return an array of annotations from a valid annot file', async () => {
        expect(true).to.be(true);
    });
}); 